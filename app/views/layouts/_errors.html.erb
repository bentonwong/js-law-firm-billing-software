<% if instance.errors.any? %>
  <div id="error_explanation">
    <h4><%= pluralize(instance.errors.count, "error") %>
      prohibited this matter from being saved:</h4>

    <ul>
    <% instance.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
